import styled from "@emotion/styled";

export const miniature = styled.img`
  border: 0.1rem solid var(--color-text-20);
  border-radius: 1.2rem;
  width: 100%;
  max-height: 40rem;
  object-fit: contain;
  object-position: center;
`;
